[project]
name = "env-pdm-pylock"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "", email = ""},
]
dependencies = ["numpy>=2.3.5", "ipython>=9.8.0", "matplotlib>=3.10.8", "pandas>=2.3.3"]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
